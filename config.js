const CONFIG = {
  network: "devnet",           // "devnet" or "mainnet-beta"
  rpcUrl: "https://api.devnet.solana.com",
  presaleWallet: "YOUR_WALLET_ADDRESS",
  tokenMint: "YOUR_TOKEN_ADDRESS",
  pricePerSol: 1000000,        // 1 SOL = 1,000,000 tokens
  targetSol: 10,               // Presale goal
  mockMode: true               // true = simulate, false = real transactions
};
